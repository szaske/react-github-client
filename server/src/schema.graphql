# import Follower from "./generated/prisma.graphql"

type Query {
  info: String!
  followers: [Follower!]!
}

type Mutation {
  newFollower(login: String!): Follower!
}

# Type is imported by comment above via graphql-yoga, even though its a comment...strange
# type Follower {
#   id: ID!
#   login: String!
# }